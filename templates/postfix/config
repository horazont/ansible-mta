# {{ ansible_managed }}

{% if item.content is mapping %}
{% for key, value in item.content.items() %}
{{ key }} = {{ value | replace("\n"," ") }}
{% endfor %}
{% else %}
{{ item.content }}
{% endif %}
